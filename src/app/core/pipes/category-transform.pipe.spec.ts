import { CategoryTransformPipe } from './category-transform.pipe';

describe('CategoryTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
